plugins {
    id 'java'
}

repositories {
    mavenCentral()
}
//тут должен быт тест какая джава используется, но он у меня за сбилдился....

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

dependencies {

    testImplementation (
            "com.codeborne:selenide:5.25.1", //selenide
            "org.junit.jupiter:junit-jupiter-api:5.8.2")  //junit
    testRuntimeOnly (
            "org.slf4j:slf4j-simple:1.7.32", //selenide
            "org.junit.jupiter:junit-jupiter-engine:5.8.2"  //junit
            )
}

test {
    useJUnitPlatform()
}

